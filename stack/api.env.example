# Example environment for the inner hummingbot-api service
# Copy to a file and mount to the launcher at /config/api.env
# (in your consumer compose: - ./api.env:/config/api.env:ro)

# Example variables (uncomment and fill as needed)
# HBOT_SOME_SETTING=value
# ANOTHER_FLAG=true
